import { SortArrayJsonPipe } from './sort-array-json.pipe';

describe('SortArrayJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new SortArrayJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
