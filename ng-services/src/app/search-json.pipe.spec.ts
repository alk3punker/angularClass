import { SearchJsonPipe } from './search-json.pipe';

describe('SearchJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
